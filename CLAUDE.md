# LyriSync

Decision log so far

- Stack: Python + PySide6, MVP first, possible Swift rewrite later touches UI only
- Spotify integration: local AppleScript polling (~300ms), not Web API
- Lyrics: LRCLIB, cache locally keyed by Spotify track ID, fallback chain synced → plain → "no lyrics"
- Architecture: three separated components (player monitor, lyrics provider, UI); monitor and provider know nothing about the UI
- Milestone order: monitor script → terminal-synced lyrics → floating window → polish
- v1 excludes: menu bar, shortcuts, focus mode, learning/translation modes, Web API, database
- Only genuine 404s are cached negatively, errors are never cached
- Prefer no lyrics over mismatched-duration lyrics
- Track identity includes URI kind (media vs track share IDs)
- Non-music items never touch the lyrics cache or network
- A line "errors surface as a retry state and re-attempt every 30s"
- Qt defaults windows to FullScreenPrimary; Primary and Auxiliary are mutually exclusive, so the all-desktops toggle must clear Primary. Native state is verified by readback, not assumed.
- Full-screen overlay requires accessory activation policy, not just collection-behavior flags; a Regular-policy app triggers a Space switch on activation. Window is unfocusable by design.
- Learning features are opt-in, toggleable layers. The default experience is always a simple synced-lyrics window; every layer off must equal the original core app.
- User-made syncs are not cache. They live in .user_syncs/ as plain .lrc, are consulted before cache and network, and nothing in the app or its docs may delete them — clearing .lyrics_cache/ must stay a safe reset.
- Tap timing = interpolated position (last poll + wall-clock since it landed) minus a reaction offset, clamped to >= 0 and >= the previous stamp. The UI thread never runs a subprocess to get a fresher position; the monitor stamps each poll instead.
- A sync is saved only when complete. Exiting early discards, and confirmation is inline (two-step control), never a modal — the window must never take focus or activate the app.
- A re-sync takes its lines from the sync it replaces, not a re-fetch: a completed pass stamps every non-blank plain line, so the stored lines ARE the song's lines. Re-syncing therefore works offline and after .lyrics_cache/ is cleared. LRCLIB's own timings are never offered for overwrite — only the user's.
- A sync pass is modal and user-driven: a fetch landing under it must not tear it down. The result is held and becomes where cancelling lands.
- Accessory activation policy is permanent and unconditional, applied before any window exists. It is no longer coupled to the all-desktops toggle, which now owns only collection behaviour and window level. There is no regular policy to fall back to, so nothing can bring the Dock icon back.
- One QMenu serves both the menu bar item and the window's right-click menu — two menus could drift apart, one cannot. Its structure is built once and never rebuilt; refresh only flips visibility, check marks and labels, so the native menu bar item never flickers. Checkable entries connect to triggered, not toggled: refresh calls setChecked on all of them.
- The menu bar item is the way back from a hidden window, so quit must be visible in every state and hiding must leave the monitor, loop and any sync pass running.
- QSettings location is injected into LyricsWindow, not global. QSettings.setDefaultFormat/setPath are process-wide and silently do nothing on macOS — trusting them meant tests writing into the real user's preferences.
- Tests must never reach the real Spotify. Entering a sync pass dispatches a seek-to-0 and a resume, so the Qt tests stub PlayerCommandTask/SeekTask/SpeakTask as well as the monitor thread; otherwise running the suite restarts whatever the developer is listening to.
- SESSION RULE: tests must never touch real user state, the network, or Spotify. Settings are injected; player commands, speech, and lyrics fetching are stubbed. Four escapes were found by their symptoms rather than by the suite — real QSettings writes, real player commands, a tray test that never ran, and a live LRCLIB fetch that aborted CI — so the seams are now backed by guards in tests/conftest.py: outbound sockets, subprocess, a bare LyricsProvider, and QSettings("lyrisync", "lyrisync") all refuse.
- A guard must record, not merely raise. Every escape happens on a worker thread, and the app catches broad exceptions there by design (a failed fetch is a retry state, not a crash), so an exception alone is swallowed exactly where it matters. Each block is recorded and an autouse fixture fails the test that caused it. The guards have tests of their own — an unrun guard is the tray test again.
- A QThread destroyed while it is still running is a qFatal: the process aborts (exit 134, "QThread: Destroyed while thread is still running") and names whichever test was on screen, which is how a shutdown bug came to look like a bug in the quit test. So _shutdown drains everything the window owns — monitor thread, then the worker pool — before anything of the window's is destroyed, and the Qt fixture forces any surviving thread down so a regression fails with a sentence instead of a signal.
- Shutdown's waits are bounded and honest. A worker that will not return in 3s (`say` can hold a line for a minute) is logged and left to the thread pool's own destructor, which is where it was always going to be dealt with; blocking quit on it would be worse than the warning.
- Legibility over an arbitrary background outranks the material. The scrim alpha is measured, not chosen by eye: the sung line must clear 4.5:1 with no blur behind it at all, so a material that fails to attach or renders pale costs depth and never readability.
- The vibrancy material is an NSVisualEffectView slid under the Qt content. The opacity gesture needs no plumbing: it rides on the NSWindow's alpha, which dims the material too.
- SUPERSEDES the pinned-dark material: the window follows the system appearance in both directions, live. Pinning dark was right only while the text was always white; now the palette inverts with the mode, so hudWindow in Aqua is the correct pale panel. The appearance is still set explicitly rather than left to inherit, for a new reason — the scrim painted on top comes from the palette this app resolved, and one answer must drive both or they disagree mid-transition.
- Following the system is Qt's colorScheme/colorSchemeChanged, not a pyobjc KVO observer or a distributed notification. The cocoa plugin already watches NSApp.effectiveAppearance and republishes it, so it costs no pyobjc and cannot disagree with Qt's own palette. Measured: the signal fires immediately on a real appearance change, and the app followed it mid-run with no relaunch (three screenshots dark, three light, one process).
- macOS's Auto transition is not a separate path. Flipping dark mode via System Events leaves AppleInterfaceStyleSwitchesAutomatically at 1 — measured — so the scheduled change and a manual one are the same effectiveAppearance change, and nothing in the code knows about sunset.
- Which backdrop is worst FLIPS with the mode, and missing that is how a light mode ships broken. White text on a dark scrim is worst over a WHITE page; dark text on a light scrim is worst over a BLACK one. Each palette is measured against the backdrop that suits it least: dark crosses 4.5:1 at alpha 147 and ships 150, light crosses at 131 and ships 134.
- Parity is pinned role by role, not just for the sung line. Every role that recedes in dark mode must recede no further in light mode, in each one's own worst case — four light colours (both accent blues, the idle control, the warning red) were swept until they satisfied it rather than picked by eye. Only the sung line carries 4.5:1; the parity test is what stops "meant to recede" becoming "gone" on one side only.
- Measured from real pixels, both modes, material rendering: dark reads 9.3:1 / 16.0:1 / 8.9:1 over a white document, a dark editor and a bright video; light reads 15.2:1 / 10.3:1 / 15.5:1. Floors without the material are 4.70:1 and 4.72:1.
- The screenshot harness needs two things that were wrong the first time and looked fine: an accessory-policy app cannot own a Space, so a showFullScreen backdrop never comes forward and every shot silently catches the desktop instead (all three captures were byte-identical); and the capture rect must come from the NSWindow frame converted out of Cocoa's bottom-left origin, because Qt's geometry disagreed with the screen by the menu bar.
- No appearance toggle. macOS already answers this question and a setting would be a second source of truth for it — the same argument as the login item's tick following the system.
- THE GOVERNING RULE for the album-colour tint: the artwork supplies a HUE and nothing else. Its own luminance and saturation are discarded and replaced with ours, per mode. The failure this prevents is the obvious implementation — sample a colour, paint with it — which works for three albums and then meets a neon cover or a near-white one.
- The tinted background is solved by BISECTION onto the untinted colour's relative luminance, not by holding HSL lightness. The two are not the same quantity and disagree badly across hues (pure yellow and pure blue sit at the same HSL lightness and nowhere near the same luminance), so pinning the wrong one would have moved the contrast floor by hue — the exact bug a hue-only tint exists to avoid. Measured: sweeping all 360 hues at full saturation moves the scrim's luminance by under 8% (that residue is 8-bit quantisation) and costs 0.03 of the floor — 4.70→4.67 dark, 4.72→4.69 light.
- Because luminance is pinned, character is free: sweeping tint saturation 0.60→1.00 moves the worst-case contrast by 0.01. So the saturations are high (0.85 dark, 0.95 light) rather than timid — the usual trade of character against legibility is simply not being made. Short of 1.00 only because full saturation pins a channel to 0 or 255 and reads as electric.
- Only the two backgrounds are tinted; every text colour keeps the value 12a measured. That is what lets the floor be re-CHECKED under tint rather than re-derived, and the parity work from 12a stays valid untouched.
- Measured with a deliberately hostile fully saturated cover, material rendering: dark 9.3:1 over a white page and 17.2:1 over a black one; light 15.2:1 and 9.7:1. Analytic floors with no material at all, swept across every hue: 4.67:1 and 4.69:1.
- The artwork line gets a try of its OWN nested inside the snapshot script's existing one. Appended to the same statement, a Spotify build that would not answer `artwork url` would fail the whole expression and take the six track fields with it — the app would show a running player and never find a song, for the sake of a colour. Nested, the cost is one missing line. artwork_url is deliberately not part of track_key: a cover arriving a poll later must not read as a different song.
- Extraction is a dominant-hue vote, not an average — averaging a cover gives mud (a red sleeve on black averages to dark maroon; a photograph averages to grey). Near-black, near-white and low-saturation pixels are discarded before voting, because nearly every sleeve is mostly dark and letting those count makes every album the same colour. A cover with no usable hue is a real answer: no tint, rather than a hue invented from noise. Confirmed live — the cover for "I'll Show You" is a black-and-white photograph, median saturation 0.005, and correctly produced no tint at all.
- The cache holds the DERIVED COLOUR, never the image: three integers of JSON per track against 100KB+ per cover, and nothing ever needs to look at one twice. "No usable hue" is cached like a definitive 404; a failed fetch and an image that will not decode are never cached, which is why decode_colour raises for one and returns None for the other rather than conflating them.
- Album colour is ALWAYS_VISIBLE, unlike the learning layers. Those hide because they cannot act (nothing to romanise without hangul); this one can always be answered and is a standing preference about the window, so appearing and vanishing with each track would hide it exactly when the user goes looking — before the music starts. Default off, like every layer.
- test_user_sync_safety's "only one module writes" check now permits artwork.py, and pays for it with a stronger claim: artwork.py may not so much as mention the user-sync directory. A module that learns to write has to argue for itself there first.
- Milestone 13 was a feel pass: no new behaviour, four changes to how the window reads. Lines rise as they change, the panel has an edge and a shadow, the album tint is restrained, and the type has a real hierarchy.
- Vertical motion is ONE signed property, not separate opacity and offset ones: `progress` runs -1 (gone, drifted up) through 0 (in place) to +1 (not yet arrived, below), with opacity = 1-|progress| and offset = progress x travel. They are not independent — a line half faded is half travelled — and one QPropertyAnimation per line change beats a parallel group of two.
- It is a QGraphicsEffect, not a moved widget. `_current_box` lives in a QVBoxLayout, so anything that moved it would be undone by the next layout pass and would ripple into the rows above and below. Drawing the source pixmap at an offset touches no geometry, which is why the rest of the window cannot feel it happening. boundingRectFor must grow by the travel or the moving block is clipped to its own box and reads as dissolving at the edge.
- The line change is 260ms per phase, extended EARLIER rather than finishing later: the arrival still settles exactly on the timestamp, it just starts moving 520ms before it instead of 200ms. Every constant derives from the one phase length so the swap point and the total window cannot drift apart from it. Sine rather than cubic easing — cubic's ends are steep enough that even 260ms reads as a flick — still In leaving and Out arriving, so the outgoing is fastest exactly where the incoming picks up and the two phases read as one movement. Travel went 7 to 10px: distance and duration are a pair, and a distance that read as a twitch at 100ms reads as too little at 260ms.
- A gap shorter than the choreography gets a QUICKER version of the same movement, not a truncated one: each phase takes at most half the gap. Both phases still fit before the timestamp and the arrival still lands ON it, at any tempo — a 400ms gap gets 200ms phases, a 120ms gap gets 60ms. The animation reads that per-transition duration rather than the nominal constant, or the clamp would be decorative and the movement would overrun the line it belongs to.
- CPU per line change grew with the duration, as expected and measured: 1.7ms for the text swap, 57.8ms for the fade, 92.7ms with the rise — against 34.1ms at the old 100ms pacing. That is 2.3% of one core at a line every four seconds, and 0.16% between lines.
- The anticipatory schedule stays authoritative and unchanged. The rise ENDS at the line's timestamp rather than starting there — motion completes on time. Eased both ways and differently: InCubic leaving (accelerates away, reads as departure), OutCubic arriving (decelerates onto its mark, reads as settling).
- QWidget::grab()/render() does NOT apply QGraphicsEffect — measured, after it produced nonsense readings. So an effect can never be verified through grab(); the reliable readback is tracing draw() itself. A real line change calls it 8 times per phase with the eased progress sequence, which is a stronger check than any screenshot.
- macOS's window server hands back a STALE frame on the first capture after a change, so frozen single frames of an animation are not trustworthy — three attempts at a filmstrip disagreed with each other. Static states screenshot fine; motion has to be measured, not photographed.
- Measured CPU per line change: 6.7ms for the text swap, ~26ms for the fade that already existed, ~1.3ms for the rise this added. At one line every four seconds that is 0.85% of one core, and the rise's own share is 0.03%.
- The hairline is ONE DEVICE pixel (1.0 / devicePixelRatioF), not one logical pixel: at 2x those differ by a factor of two and a two-pixel line is a border rather than an edge. Inset by half its own width so it lands inside the fill — straddling the boundary would put half the stroke outside the material's mask and read as a second, softer edge beside the first. Palette-owned, and deliberately never tinted by the album: a coloured hairline reads as a border.
- The shadow is the native NSWindow one (setHasShadow_), not a painted one: macOS derives the shape from the alpha channel so it follows the rounded corners for free, sits outside the window bounds where a painted one would have to eat into the panel, and costs the compositor nothing per frame. It is CACHED, so resize must call invalidateShadow or the old silhouette stays. Verified it survives the opacity gesture — at alphaValue 0.5 hasShadow is still true.
- The tint is specified as CHROMA of the finished panel (0-255 channel spread, after the background's alpha), not as HSL saturation. Diagnosed, not guessed: at 0.22 saturation the light tint moved the scrim by 3/255 — LESS than the palette's own built-in blue cast — so album colour looked switched off in light mode however strong the cover. The cause is that saturation collapses at both ends of the lightness range: the light scrim at L=0.973 can only reach 14/255 chroma even at S=1.0, while the dark scrim at L=0.067 gets 34/255 from the same number. One saturation therefore meant 2.4x more colour in dark than light. Stated as chroma the constant means the same thing in both modes.
- The saturation that yields a wanted chroma is found by BISECTING on saturation against the chroma actually achieved — not by inverting HSL's closed form. The closed form answers a different question (chroma at some lightness), and pinning the luminance then moves the lightness by wildly different amounts per hue. Feeding that back as a correction does not converge, it OSCILLATES: yellow alternated between chroma 204 and chroma 2 on successive rounds, and stopping after two landed on whichever the parity chose — which is how a yellow cover came out less coloured than no cover at all. Chroma rises monotonically with saturation at fixed luminance, so bisection converges and a hue that cannot reach the target settles for its most.
- Light mode carries genuinely less tint than dark, and it is the gamut, not a tuning failure. At the light panel's luminance 0.93 the hues that carry little luminance of their own (blues, magentas, reds) are nearly white — buying their colour costs brightness the pinned luminance will not give up. Delivered: dark 10.0-14.1 (median 13.5), light 4.7-12.1 (median 8.4). Relaxing the luminance was measured rather than assumed: a 5% drop buys the worst hue only 4.7 to 7.9 chroma and leaves the floor at 4.52, with none of the rounding headroom the rest of the palette keeps. So luminance stays pinned.
- Every hue must beat the UNTINTED panel's own chroma, in both modes. That is the test that would have caught the original bug, and it is the one that caught the oscillation too.
- CONTRAST HEADROOM IS NOT AESTHETIC HEADROOM. 12b set tint saturation to 0.85/0.95 because measurement showed saturation was nearly free; the reasoning was sound and the conclusion was wrong. What the floor permits and what looks like a pane of glass are different questions, and a near-fully-saturated wash answers only the first. Now 0.18/0.22, set by eye at the point where the colour is felt rather than noticed, with the floor re-checked afterwards rather than used to justify the value (4.68 dark, 4.69 light — unchanged).
- Qt stylesheets have no letter-spacing property, so tracking is the one type setting that cannot live with the others. It goes on via QFont, with the font read back AFTER the stylesheet has been applied and polished — measured that size and weight still come from the stylesheet afterwards, so typography.py stays the single source and this only adds what the stylesheet cannot say. Only the sung line is tracked; tightening body-size type costs legibility and buys nothing.
- The type hierarchy is 20/700 against 13/400 — a 1.54x size ratio and three weight steps, where it was 18/600 against 14/400 (1.29x and one step). At the old separation the eye had to read the window to find the current line. The floor grew 161 to 183 as a result, so the default window height went 170 to 200: a first run should open at a shape it chose, not at the floor it was clamped to.
- A test may not patch `window._pool` — it is QThreadPool.globalInstance(), a process-wide singleton, so assigning to its start() leaks into every test that runs afterwards (it broke three shutdown tests two files later). Record what a window starts with a recording subclass of the task instead.
- Blur and window translucency are mutually exclusive: macOS renders the behind-window blur only while alphaValue is exactly 1, and dimming either native view instead of the window does not save it. So the window starts fully opaque and dimming is understood as spending the frost to see through. Verified by screenshot, not by API docs, which say none of this.
- Screenshots are the readback for anything the compositor draws. _material is not None only proves the view attached; whether it blurs is a question about pixels, so the check is a capture over a text-heavy backdrop — sharp glyphs inside the window mean no blur however healthy the readback looks.
- typography.py owns the type scale and geometry.py imports it, so the height floor can never describe a type scale the stylesheet has moved on from.
- No colour emoji in the window. 🔊 renders in colour whatever is asked of it and macOS has no monochrome speaker glyph (U+1F56A and friends are tofu), so the speak button is ♬; 🎤 takes U+FE0E and goes monochrome.
- SUPERSEDES the line above for the speak button: the native answer is SF Symbols, not a hunt for a Unicode character that happens to render monochrome. `text.bubble` via NSImage.imageWithSystemSymbolName, tinted here because a template image carries its shape in the alpha channel and takes its colour from us. ♬ stays as the fallback for when symbols cannot be had (off macOS, no pyobjc, symbol missing) — symbols.py returns None and the button keeps the text it was built with, which is what lets the suite stay headless. Rendered per scale at the screen's device pixel ratio: an SF Symbol is drawn for the size it is asked for, and a 1x glyph stretched to 2x is visibly soft.
- One source for control colours: window.py owns _CONTROL_IDLE/_HOVER/_ENGAGED, the stylesheet paints text with them and symbols.py tints the icon with them, so the glyph and the icon that replaced it cannot describe different states.
- Packaged with PyInstaller, not py2app: PySide6 and pyobjc are both covered by hooks PyInstaller maintains upstream, where py2app needs hand-written recipes for the same bundle. `make app` is the whole build — icon, freeze, ad-hoc signature.
- The bundle identifier IS the settings contract: com.lyrisync.lyrisync is what QSettings("lyrisync", "lyrisync") already resolves to, so the bundled app and a terminal run share one plist. Verified by launching both at once and reading the window geometry back — same position, same size, same file.
- LSUIElement in Info.plist as well as apply_accessory_policy() at runtime. The runtime call is what the code guarantees; the plist key is what stops macOS showing a Dock icon for the instant before it runs.
- NSAppleEventsUsageDescription is load-bearing, not paperwork: without it macOS refuses the AppleScript calls silently and the app looks broken — window up, menu bar item present, never finds a song.
- Open at Login uses SMAppService.mainApp, not a LaunchAgent plist, because the menu entry has to show what the system thinks and only SMAppService can answer that: a plist stays exactly as written after the user disables the item in System Settings, so a menu built on it would keep claiming the app starts at login when it no longer does. The cost is macOS 13 (the app's floor is 11); below that the entry is not offered, which beats a fallback that cannot describe itself honestly on the very systems it would serve.
- The login item entry is never drawn from the stored preference. The setting records what the user asked for; status() is re-read at startup and on every menu opening, and the tick follows macOS. They are only ever compared to log the disagreement.
- REQUIRES_APPROVAL is not enabled. Registration can return no error and still not start the app at login, so set_enabled re-reads the status instead of trusting its own return, reports failure, and the entry stays unticked with a label naming System Settings — an unticked box that explains itself beats a ticked one that is wrong until next login.
- Measured, not assumed: a freshly built bundle reports NOT_FOUND (3) before it has ever been registered, registers cleanly under an ad-hoc signature with no approval step, and reports NOT_REGISTERED (0) after being switched off. So NOT_FOUND is an ordinary "off" and the entry is still offered from it. Verified by a throwaway probe built into a scratch bundle, registered and unregistered in one run, then reverted.
- Every native login-item call goes through login_item._main_app_service(), one door, so tests have one seam to block. A stray call would leave a real login item on whoever ran the suite.
- Bundle building stays out of CI. The artefact is macOS-only and what matters about it (menu bar icon, no Dock icon, the Automation prompt) can only be accepted by a person, so a green tick would be claiming more than it checked.
- No test is macOS-only. Everything native is guarded off-cocoa in the code and asserted structurally, so the whole suite runs headless on the offscreen platform. CI must apt-get PySide6's system libraries (libegl1, libgl1, libxkbcommon-x11-0, ...) — the wheel imports fine without them and only fails at load with "libEGL.so.1: cannot open shared object file", which pytest.importorskip does NOT skip by default (it is an ImportError, not a ModuleNotFoundError).
- The global hotkey is Carbon's RegisterEventHotKey, not a CGEventTap or an NSEvent global monitor. Those two read every keystroke on the machine, so macOS puts them behind an Accessibility prompt the user has to leave the app to answer; the event manager matches one combination itself and calls back only when it fires. Deprecated, but nothing replaced it, and this is what every menu bar utility with a shortcut and no Accessibility prompt is doing.
- pyobjc has no Carbon bindings, so it is ctypes — measured, not assumed: RegisterEventHotKey/InstallEventHandler/GetApplicationEventTarget all resolve, registration returns noErr with no prompt, and the one missing symbol (NewEventHandlerUPP) is a 64-bit no-op that returned its argument, so the ctypes callback IS the UPP. Verified again in the real app: "global hotkey ⇧⌘L registered" under the accessory policy, released on quit.
- Every native hotkey call goes through hotkey._carbon(), one door, same as login_item. A stray registration in the suite takes ⇧⌘L from whoever is running it, for as long as the process lives. Out-params go through ctypes.pointer, not byref, so the door can be faked and the register/press/release sequence tested for real.
- RegisterEventHotKey is NOT exclusive across apps — measured, and it corrected the code: two LyriSync processes both claimed ⇧⌘L and both got noErr, with macOS deciding between them per press. eventHotKeyExistsErr (-9878) comes back only when the SAME process already holds the combination. So a refusal is never "another app owns it", and neither the log line nor the README may say it is; the honest message names the OSStatus and points at the menu bar item.
- The hotkey drives _set_lyrics_visible — the menu entry's own setter — so there is one piece of state and the tick matches the window whichever was used. It is deliberately NOT shown on that menu entry: a QAction shortcut would be a second mechanism firing one action, and a label printing ⇧⌘L while another app owned the combination would be the login-item failure again, an entry claiming something untrue. The README carries the keys; the log says whether they landed.
- The hotkey is ⇧⌘J, moved from ⇧⌘L when that collided with something the user already runs. The move was one constant and its label, which is the argument for keeping it in one place. Key codes are verified against the live layout rather than read off a header: CGEventKeyboardGetUnicodeString on an event built from 0x26 answers "j" — and building a CGEvent needs no Accessibility permission, only posting one does.
- The monitor's stop is a threading.Event set once and never cleared, not a flag run() raises on entry. The old shape lost any stop() that landed between starting the thread and the thread body beginning — found while building the hotkey, not by the suite: it showed up as a 3s stall in test_visibility_survives_the_shutdown_save (every run) and an intermittent "monitor thread outlived _shutdown" in teardown, which is one bounded wait away from the qFatal. The remaining poll interval is now waited on rather than slept through, so quitting does not first sit out a poll: a real run quits in 0.3s and the suite went 4.2s → 1.3s.
- Releasing the hotkey is the FIRST thing _shutdown does. It is the only thing left that can still call in — Carbon holds a pointer to a callback that toggles a window being torn down. Unregister is idempotent (shutdown is reached more than once) and the ctypes callback object is dropped only after the handler is removed, because until then C holds a pointer into it.

- The line change played TWICE because the choreography (520ms) outgrew the poll interval (300ms) in 13.1, so a poll lands inside almost every change. Two separate causes, both needed fixing: the poll re-armed both timers from what was LEFT of the gap (a smaller eta, hence a hurried second run), and — the visible half — `_on_position_update` read the predicted swap's one-line lead over the view model as a missed prediction and re-rendered the line being left. Reproduced deterministically before fixing: the indices put on screen were [1, 0] where they should read [1].
- Dedupe is by TARGET LINE INDEX, the same shape as the view model's track identity: an announcement of what is already showing is not news. It is two questions, not one — `may_arm(target)` stays true until the movement begins, so an ordinary poll keeps re-deriving the schedule from a fresh position and a seek is still picked up within one poll; `begin(target)` is claimed by the fade-out, the moment the change stops being scheduled and starts happening, and refuses every later trigger for that line.
- Being ahead of the view model needs a BOUND, not just an owner. Suppressing the snap whenever the screen leads by one line would leave the window sitting on a line the song has left after a seek backwards into the middle of the current one — it would wait for a timestamp half a verse away. So `leads()` also asks whether the player is still within the choreography plus one poll interval of that timestamp; further than that is the world moving, and it snaps.
- SUPERSEDES "the hairline is deliberately never tinted by the album". The panel has no gamut left — its luminance is pinned by the 4.5:1 promise, and in light mode buying colour would cost brightness the floor will not give up. The hairline has no text on it, so it has no contrast obligation, and that is where the album colour now lives: the panel keeps its restrained wash and the edge carries the hue at ~4x. The old objection was that a coloured hairline reads as a border; measured against real covers in both modes it does not, because it is still one device pixel and still inset.
- Freed of the luminance pin, the hairline pins HSL LIGHTNESS instead, and that buys the one thing the panel can never have: at a fixed lightness an HSL colour's chroma is exactly saturation x (1 - |2L - 1|), with NO HUE TERM. So the edge is solved by arithmetic rather than bisection and delivers 46.2 units for all 360 hues in dark and 45.7 in light — identical across hues, and the same in both modes — where the panel bisects per hue and still lands 10.0-14.1 dark, 4.7-12.1 light. Stated as delivered chroma (after the edge's own alpha), the same convention as the panel, so the two numbers can be compared.
- What FIXES the two lightnesses is that a coloured line is not automatically an edge: it must stay lighter than the dark panel and darker than the pale one, for every hue, over the backdrop that leaves the panel closest to it. The binding cases are blue in dark mode and yellow in light — the hues furthest from their own panel in luminance — and they put the values at 0.72 and 0.30 (worst separation +0.0386 and +0.0053). Alpha (110/105) is a free knob against saturation for the same delivered chroma; it is what lets a strongly coloured edge not be a strongly saturated one.
- Contrast floors re-checked, not re-derived: 4.677 dark and 4.692 light swept over all 360 hues, identical to 13.1's. Nothing else could have happened — the tint touches only `border`, and the test that says so is the one that used to say only scrim and solid moved. Measured from real pixels with the material rendering, over each mode's worst backdrop: 11.4-11.6:1 dark and 11.9-12.0:1 light, with the tint moving it by under 0.3 in the safe direction.
- The window shadow is NOT tinted, and it is not a judgement call: NSWindow exposes hasShadow/invalidateShadow/setShadowStyle_ and nothing about colour (the shadow is drawn by the window server from the alpha channel; every colour-adjacent symbol is private). Tinting it would mean giving up the native shadow for a painted one, which is exactly the trade 13 refused — a painted shadow sits inside the window's bounds and costs the compositor per frame.
- A screenshot harness must PUMP the event loop, never sleep in it. `time.sleep` inside the slot doing the capturing blocks Qt from repainting, so every capture comes back byte-identical and the whole run looks like the stale-frame problem — twelve shots, six distinct pairs, all wrong. Spin processEvents in a loop instead, and keep throwing the first capture away.
- The tinted edge and the tinted panel ride ONE cross-fade mix. Two animations of the same tint could only drift apart, and an edge that changed colour before its panel would read as a flicker at the rim.

- The bundle's version comes from importlib.metadata.version("lyrisync") — the INSTALLED distribution, not a re-read of pyproject.toml and never a literal — because the installed package is what PyInstaller freezes. Reading the file would describe the source tree, which is the same thing right up until it is not. Both Info.plist keys (CFBundleShortVersionString, Finder's, and CFBundleVersion, the one macOS compares) and BUNDLE's own version argument come from that one name.
- Installed metadata has exactly one failure mode and the build refuses on it: an editable install's version is a snapshot taken at install time, so editing pyproject.toml without reinstalling leaves it stale, and the bundle would quietly claim the old number. The spec compares the two and raises SystemExit — measured, with the output path redirected so the good bundle survived the experiment: pyproject at 1.0.1 against an installed 1.0.0 stopped PyInstaller before it wrote anything, naming both versions and the reinstall command. The suite carries the same comparison as an ordinary red test, so drift is found at `pytest` rather than at release.
- A PyInstaller spec cannot be imported to be tested — it runs with Analysis/EXE/BUNDLE and SPECPATH injected into its globals — so test_packaging.py reads it as an AST and asserts where the number comes FROM, never what it is. A test pinning the value would be the second copy of the version all over again. It resolves whatever name the spec imported importlib.metadata.version as, follows it to the binding, and requires both plist keys to reference that same name; the only version-shaped literal permitted anywhere in the file is LSMinimumSystemVersion, which is the macOS floor and moves for entirely different reasons.

- The version now has exactly ONE definition for the whole project: `lyrisync/__init__.py` resolves it from importlib.metadata and builds USER_AGENT from it, and artwork.py and lyrics_provider.py import that string rather than spelling it out. Both used to carry "lyrisync/0.1.0 (…)" by hand, and the bump to 1.0.0 made both wrong at once — the app introduced itself to LRCLIB as a version it had not been since the previous commit. The URL inside the string is carried over untouched; only the version was in question.
- copy_metadata("lyrisync") in the spec is LOAD-BEARING, not housekeeping. PyInstaller freezes the package WITHOUT its .dist-info unless told otherwise — verified by finding none in a built bundle — so `__init__` would fall back and the frozen app would introduce itself as "unknown" while the Info.plist beside it said 1.0.0. Verified after adding it, by resolving the version with importlib.metadata scoped to the bundle's own Frameworks path (`distributions(path=[…])`, so the installed copy cannot be the one answering): 1.0.0, from the bundle alone.
- The missing-metadata fallback is the string "unknown", deliberately not a number. A fallback version literal is precisely the drift being removed, and an app that cannot tell you its version is better than one that tells you the wrong one. It is only reachable from an uninstalled source tree.
- Scanning for a hardcoded version means scanning for a SUBSTRING, not a whole-string literal: the copy was buried mid-sentence in "lyrisync/0.1.0 (…)", exactly where a whole-string check walks past it. Docstrings are excluded from that scan, because the LRC parser documents "[00:12.00][00:55.30] chorus" and a scan that counted that would be satisfiable only by deleting the documentation.

- The contact URL in the User-Agent was WRONG for thirteen milestones — github.com/matthewhuang, where the repository is github.com/matt-huang1 — so LRCLIB was being handed an address that 404s, which is the one thing a contact URL exists to not do. It survived that long because nothing compared it to anything; consolidating the User-Agent into one definition is what made it a single edit rather than a hunt.
- It is now pinned against the README's `git clone` line, deliberately not `git remote`: a remote passes on anybody's fork and is not reliably available in a CI checkout, while the clone line is the address a real person is told to use and therefore the one that gets noticed when it breaks. The URL is its own constant (REPOSITORY_URL) so the test has something to compare that is not a substring search of a sentence.
- Verified in the artefact, not just the source: the frozen bytecode was read back out of the rebuilt bundle's embedded PYZ (PyInstaller 6 puts it inside the executable) and its lyrisync/__init__ carries the corrected URL. A source tree that is right and a bundle that is stale look identical from the outside.
- `make app` fails intermittently with "rm: dist: Directory not empty" — Finder recreating .DS_Store between rm's unlink pass and its rmdir, whenever the folder is open in a window. It has happened twice. A retry always works; the clean step is simply not atomic against Finder.

- Milestone 14: per-app window position memory. The window returns to wherever the user last put it for whichever app just came to the front. Opt-in, default off, and off removes the notification subscription rather than merely ignoring it — the layers principle means the app is not watching what the user does at all with the layer off.
- Learning is IMPLICIT and there is no save action: the only moment the user says anything about where the window belongs is when a drag or resize ends, so that is the only moment anything is recorded. It works at all because of a property the app already had — the window is unfocusable and the app is an accessory, so dragging it does not change which app is frontmost. Without that, every drag would record a position against LyriSync itself.
- NSWorkspace's activation notification needs NO permission, which is the whole reason it is the mechanism: an Accessibility observer or window-list polling could answer the same question and both put a prompt in front of the user for something macOS already broadcasts. Block-based observer rather than an NSObject subclass — no state to hold on the Objective-C side, and no class to define at import time on a machine with no pyobjc. One door (frontmost._workspace), and a test asserts NSWorkspace is imported in exactly one place INSIDE it, because a second import would pass every behavioural test while reopening the door.
- THE BUG THAT ONLY A REAL RUN COULD FIND: the QTimer that wakes the app and the debounce rule that decides whether an app has settled are two clocks measuring one interval, and QTimer is entitled to fire early. Measured live at 390ms against a 400ms rule — the rule said "not yet", and because the timer is single-shot the arrival was DROPPED FOR GOOD, so the window failed to follow on roughly half the switches. Not fixed with a fudge factor on either side: the rule stays authoritative and the timer is only a prompt, re-arming for whatever is actually left. Same shape as the monitor waiting out the remainder of a poll interval rather than sleeping through one.
- The self-filter in may_learn earned its place immediately and by accident. The first verification harness did not call apply_accessory_policy() before creating the window, so the process was a Regular app, show() activated it, and OUR OWN bundle id arrived as the frontmost app. The filter refused it, so the symptom was positions quietly not being learned rather than a map full of entries keyed on ourselves. A verification harness has to mirror the real app's startup or it is measuring a different program.
- Doing nothing is the answer for an app with no remembered position. A default would move the window somewhere the user never put it, which is worse than leaving it alone — the same instinct as preferring no lyrics to mismatched-duration lyrics.
- Positions are CHEAP: one drag each to relearn. That is what licenses a crude LRU cap (50) and a Forget entry with no confirmation, and it is the whole difference between this map and a hand-made sync. Recency counts a use rather than only a write, or the app you switch to constantly but rarely re-place would be the first evicted.
- Stored as a JSON list of triples, not an object, so eviction order is part of the format rather than a property of whichever JSON reader loads it. One bad entry costs only itself and unreadable text yields an empty map — a hand-edited settings file should degrade, not stop the app starting.
- The window move is 260ms InOutSine — one phase of a line change, sine for 13's reason, InOut rather than In-then-Out because this is one continuous movement rather than two phases handing over. A second activation mid-travel retargets from where the window GOT TO, which is the tint cross-fade's rule and for the same reason.
- Restoring this layer at startup assigns the flag directly instead of calling its setter, unlike every other path. _restore_settings runs BEFORE _build_menu and the setter refreshes the menu — routing through it crashed the app on any launch with the layer already on, which is a bug the suite caught only because a test restored a saved-on state.

- The README's install section leads with BUILDING FROM SOURCE, and the front page no longer asks a visitor to click through a security warning on somebody's word. The release download is the convenience option, second, with the SHA256 and the one-line command to check it — a download that can be verified rather than trusted. The click-through steps are deliberately not on the front page: getting past Gatekeeper is a decision, and a decision belongs on a page that explains it (docs/gatekeeper.md), not in a numbered list a reader follows without reading.
- The published hash is computed from the BYTES, not copied from GitHub's `digest` field: 52f7ac2b… for LyriSync-1.0.0.zip, downloaded and run through shasum, matching what the API claimed. Publishing a hash somebody will check against and taking it on faith from the same service that serves the file would be missing the point twice.
- Verified rather than asserted, because the whole page is about not asking for trust: a locally built copy carries only com.apple.provenance and no com.apple.quarantine, its signature reads `Signature=adhoc / TeamIdentifier=not set`, and `spctl -a -t exec` REJECTS it. That last one is the subtle part — spctl rejects a locally built copy too. The difference is not the signature but the quarantine flag: without it macOS never runs the assessment at launch, which is exactly why building it yourself involves no dialog.
- The quarantine flag comes from the DOWNLOADING APPLICATION, not from the network. Browsers and Mail set it; curl does not — measured, on the release zip fetched here. Worth stating because it explains why one person hits the block and another never does.
- REMOVED the "right-click → Open" advice that had been in the README and packaging.md since milestone 11. Recent macOS no longer honours Control-click → Open as a Gatekeeper override for software that fails assessment, so the documented route is System Settings → Privacy & Security → Open Anyway, with `xattr -d com.apple.quarantine` as the explicit alternative. The old advice was not merely stale, it was advice that does not work on the machine this app is developed on.
- Notarisation is described in terms of what it WOULD and WOULD NOT tell a reader — an automated malware scan plus an accountable identity, not a review, not an assurance the app does what its README claims. A page that only said "trust me, it is fine" would be the euphemism this rewrite exists to remove, and so would one implying the missing notarisation makes the app dangerous.

- dist/ is now EMPTIED, never removed, in both make_app.sh and the Makefile's clean. `rm -rf dist` unlinks the contents and then the directory, and Finder recreates .DS_Store in that gap whenever the folder is open in a window, so the rmdir dies with "Directory not empty". Emptying has no such gap — a .DS_Store written a microsecond later lands in a directory being kept. Fixed BY CONSTRUCTION rather than by winning the race: there is no rmdir left to lose.
- The race is intermittent and could not be reproduced on demand — three attempts at the old `rm -rf dist` with the folder open all succeeded, and the first of them deleted the directory, which invalidates the Finder window and makes the next two a different experiment. That is why it took two real builds to surface, and why the fix is verified as "make app succeeds with dist open in Finder" rather than as "the old code fails here".
- The Makefile's clean was missing ONE thing, not three: packaging/LyriSync.iconset. build/ and LyriSync.icns were still being removed. The help text promised a full clean and now describes what actually happens ("remove build/ and the generated icon, empty dist/"), which is the half that was genuinely wrong.
- A comment inside a make recipe is echoed at the shell, so the explanation for the dist handling sits above the target rather than inside it.
- The published SHA256 lives in the README and nowhere else; docs/gatekeeper.md shows the command and points at the README for the value. A hash duplicated across two files is one that will eventually disagree with itself, and the person who notices would be someone deciding whether to trust a binary.
- ditto, never zip, and measured rather than asserted: the same 1.0.0 bundle is 36.1 MB with `ditto -c -k --keepParent` and 187.0 MB with `zip -r`, because the bundle carries 109 symlinks (Python.framework and every Qt framework have Versions/Current trees) and zip stores a full copy of each target instead of the link. The naive archive is five times the size and does not preserve the signature.
- make_app.sh's closing line no longer tells the user to right-click → Open. A bundle built there is not quarantined and simply opens; the advice was stale in the same way the README's was, and a build script contradicting the docs is the same two-sources problem in a place nobody thinks to look.

- Milestone 14.1: per-app position memory was reported as not working, AND as impossible to tell apart from working. The second half is the real defect — implicit learning with no feedback is indistinguishable from a broken feature — and fixing it is what made the first half diagnosable.
- THE BUG: dragging the window ACTIVATES LyriSync, so our own bundle id arrived as an activation, `_frontmost` became us, and `learn_refusal`'s self-filter refused every drag. The map could never gain an entry — the user's settings file said `remember_position=1` with `app_positions=[]`, which is the whole diagnosis in two values. 14's founding assumption ("the window is unfocusable and the app is an accessory, so dragging it does not change which app is frontmost") conflated KEY FOCUS with APP ACTIVATION: unfocusable is about the former, and an accessory app can be frontmost.
- Fixed where the assumption failed, not where the symptom appeared: our own activation is dropped rather than becoming the frontmost app, so a drag records against the last app that was not us. The self-filter stays — it is now a second line rather than the only one, and it is what would catch this class of thing again.
- It hid for a whole milestone because 14's harness called `_learn_position` directly. No click, no activation, no bug. The harness now posts real CGEvent presses, drags and releases at the window — the difference between testing the gesture and testing the function the gesture calls. Reproduced on demand before fixing, via a `--without-filter` flag that restores the old handler: two real drags, `map=[]`, window never follows.
- Every refusal NAMES itself, and the name is not a second copy of the rule: `learn_refusal`/`move_refusal` return the reason and `may_learn`/`may_move` are derived from them. `observe` returns ARRIVAL/REPEAT/UNKEYABLE for the same reason — the alternative is asking the debounce about its own state afterwards and reconstructing the answer, and a reconstruction can disagree with what happened.
- The menu carries a disabled readout: a count AND the frontmost app's position. Two facts because there are two ways to doubt an implicit feature — whether anything has been learned at all, and whether THIS app is one of them. It shows the bundle identifier, deliberately: it is what the map is keyed on and what the log says, so the two can be compared, and a localised name would mean reading something about the other app beyond the identifier it advertises.
- The readout's menuRole is NoRole, not Qt's default TextHeuristicRole. It is the one entry whose text the app does not write, and the heuristic that moves "Preferences…" into the application menu matches SUBSTRINGS — com.apple.systempreferences would trip it. A diagnostic that relocates itself depending on which app you switched to would vanish exactly when read.
- The readout follows the TOGGLE, not the map (unlike the forget entry). Not because it could not act: it names the frontmost app, and with the layer off nothing is watching which app that is. A stale line is worse than no line, and going to look would be the watching that "off" ends.
- `peek` beside `recall`: reading the menu must not count as using a position, or the eviction order would describe where the user has been looking rather than where they have been working.
- NO on-window acknowledgement. The only surface a transient line could take is the sung-line row, and the title card — the sole precedent for borrowing it — is about the song; to say anything useful it would have to print a bundle identifier in the middle of the lyrics; and the wordless version (pulsing the hairline) would be a second animation of an edge that 13.2 gave one owner on purpose. The menu is refreshed at the moment of learning as well as on opening, so the answer is one click away.
- Full-screen Spaces are NOT a separate path, measured in the bundle rather than inferred: entering full screen announces nothing (the app was already frontmost, so nothing activated), leaving for a windowed app and coming back both announce ordinary activations, and the window follows 400ms later. Confirmed from pixels too — a capture inside Safari's full-screen Space has the window at the position learned for Safari.
- The scripted click on the menu bar item announced no activation, and the honest reading is that this is UNDETERMINED: a status-item click may not activate an accessory app, or the synthetic click may never have landed. So the self-activation branch is justified by the drag, which is measured, and not by the menu.

- Milestone 14.2 (humanising): SUPERSEDES "it shows the bundle identifier, deliberately". That argument — the identifier is what the map is keyed on and what the log says, so the two can be compared — belongs to the LOG. A menu is read by a person deciding whether a feature works, and com.microsoft.VSCode makes them translate first. Names now, with the app's icon; the identifier remains the fallback and is still what everything is keyed on.
- The name costs nothing because it arrives WITH the identifier: NSWorkspace's activation notification carries the NSRunningApplication, so `_identity()` takes both at once. Asking later would mean walking the running-application list on every menu refresh (three times a second) and would have no answer at all for an app that has since quit.
- So the name is STORED beside the position — the map outlives the sessions that taught it. Last-seen wins, and a save that does not know the name does not erase the one already there: not knowing this time is not evidence that last time was wrong. The stored row went from three fields to four and the three-field shape is still read, because refusing it would cost the user every position they had to gain a label.
- Coordinates are OUT of the readout. A number pair cannot be checked against anything by eye and the window is sitting at it in plain view; they stay in the DEBUG log, where a reader is comparing them with something. A detail toggle to put them back would be an extra entry for a fact already on screen.
- Icons: `iconForFile_` hands back every representation from 16 to 1024 — measured at 74MB of TIFF decoding to 1024x1024, which is not a menu icon. Drawn once at the size asked for it is 12KB and comes back at the screen's own scale (32px for 16 points), and the DPR is derived from pixels/points rather than from the screen. They cross out of frontmost.py as BYTES so no pyobjc object escapes the one door; symbols.py decodes them and does NOT tint them — an app's icon is its own artwork.
- The readout's icon is fetched only when the frontmost app CHANGES: _refresh_menu runs on every render, and drawing an icon is two calls into AppKit.
- "Remembered apps" is the one menu whose CONTENTS are rebuilt rather than only relabelled, and the exception argues for itself: a list of what has been learned cannot be a fixed set, and it is assembled on its own aboutToShow — only while the user is looking at that submenu, never while the menu bar item is idle. Per-app forget lives there; forget-all stays where it was.
- macOS does not draw a QMenu SECTION's text — found by opening the menu and seeing four apps and no heading. The hint is a disabled action instead, and it sits first so it explains the list before the click rather than after it.
- SUPERSEDES "NO on-window acknowledgement". Two of the three objections stand (no text on the window, no bundle identifier mid-lyric); the third was too strict. The rule that replaces it is DESIGN_PHILOSOPHY 12: transient feedback may briefly borrow a surface that persistent decoration owns, then return it — provided the return is STRUCTURAL rather than remembered.
- What makes the loan structural: the glow is a mix applied at PAINT TIME (`_painted_border`), never written into the tint state (`_current_border` stays the album's own answer). A cover landing mid-glow therefore cross-fades underneath it and cannot capture a warmed edge — which is precisely what 13.2's one-owner rule was protecting against. Giving the edge back is the animation reaching zero, not cleanup that could be skipped.
- The shape is a half sine over one property, peaking at 0.85: nothing at either end, so there is no step at either boundary and no easing curve to choose. 520ms = two line-change phases, deriving from the constant the window's motion is already built from. One per gesture — a second acknowledgement inside the first is refused rather than restarted, which is also what makes a release delivered twice harmless.
- The glow is warm because every other accent here is cool, and it is the amber echo practice already uses. Deliberately NOT tinted by the album: an acknowledgement that changed colour with the cover would read as part of the artwork rather than as an answer.
- Measured from real pixels in both appearances: dark (255,255,255,30) → (255,220,140,145) → back; with a red cover (237,130,130,110) → (252,201,122,157) → back to exactly (237,130,130,110). Traced rather than photographed (33 frames over 531ms, peak 0.85, ends 0.0) because macOS returns a stale frame on the first capture after a change. 15.9ms of CPU per drag, ~0.5ms a frame, against 92.7ms for one line change.

- Milestone 15 (menu bar presence). The learn glow was too subtle to notice without staring, and the fix was NOT more colour: at peak 0.85 the amber was still averaged with a nearly transparent hairline. Peak now 1.0, duration 520→780ms, and — the half that does the work — the edge THICKENS from one device pixel to three. A single device pixel changing colour is a few hundred pixels on a 460-point window; three pixels of warm edge is a shape change, and the eye is far better at those. Width rides the same intensity as the colour (one number, so it cannot be left thick and cool) and returns with it.
- Measured from paintEvent's pixels, 1dp → 3dp: dark (255,255,255,30) → (255,214,120,165); light (0,0,0,38) → (150,96,0,170); with a red cover the tinted edge (237,130,130,110) / (132,21,21,105) reaches the same amber, because the acknowledgement is the same answer every time. Over a real backdrop: dark (58,60,65) → (197,165,96), light (185,186,186) → (170,127,52). 49 frames over 787ms, back to the starting colour to the channel.
- Per-app forget is REMOVED from the Remembered apps list, which is now a readout of disabled rows. Re-dragging overwrites a position, so forgetting one app can only express "stop moving the window for this one app" — not a thing anybody wants for one app while wanting it for the others. Forget-all stays: "stop doing this" is a real wish.
- The window now FLIES to the menu bar item on hide and back on show, 260ms, InSine leaving and OutSine arriving (13's pairing). The CONTENT scales, not the window: a CALayer affine transform on the view Qt draws into, so the compositor scales what is already rendered. Animating the window's size would re-lay the text out every frame — the type scale follows the width — and read as the window rewriting itself.
- Qt leaves that layer's anchorPoint at its ORIGIN, so scaling about the centre needs a translation of half the shrinkage each way. Measured, not assumed: a bare scale pinned the content to the bottom-left corner.
- Two things cannot travel and are put away for the journey: the material (a SIBLING view, so it would sit at full size while the panel shrank away from it — and alpha<1 kills the blur anyway) and the native shadow (cached from the alpha channel, so it would keep a full-size silhouette around a small panel).
- The item's rectangle comes from QSystemTrayIcon.geometry(), and that IS the status item's button window: measured against NSStatusBarWindow.frame() in the same process, (1159,1073 38x34) in Cocoa is (1159,0 38x34) there, exactly. A pyobjc route beside it would be a second source of truth for one rectangle.
- No usable item — notch, overflow, unplugged display, no menu bar — is a plain fade in place, and it is the SAME function with no target rather than a second path. A fallback nobody exercises is a fallback that does not work; the suite runs offscreen where there is no menu bar, so the fallback is what runs by default and the flight itself needs a seam.
- THE ORDERING FOUND THE HARD WAY: the position must go back BEFORE the window is hidden. Moving a window that has just been hidden is undone by the platform's own move event for the last position it actually had — and with _flight_home already given up, that is how a window ends up somewhere nobody put it. Restoring while it is still on screen has no such race and shows nothing: at that point it is at zero opacity and a sixteenth of its size.
- Everything the flight borrows (position, opacity, content scale, material, shadow) is given back by ONE method, so an interruption, a landing and a shutdown all leave the same state. A reversal picks up the progress reached and takes proportionally less time. Shutdown lands the flight before saving, or the menu bar's corner would be persisted as where the user left the window. A press during a flight is refused: Qt hit-tests the full-size layout even while the content is drawn at a fraction of it.
- Startup does NOT fly. apply_saved_visibility is the app arriving, not the user asking for the window back.
- THREE menu bar states, and the number is the design: 16 points, shared with a dozen other icons, taken in without focusing. idle (40% ink — nothing playing, or lyrics hidden), active (full), practice (a dot — loop, echo or a sync pass). They differ in how much ink there is and whether a mark is present, which are the two differences that survive being small. Idle is the ACTIVE glyph with less ink, not a second drawing: one shape at two strengths is one icon doing more or less.
- Practice outranks everything including the window being hidden — a pass keeps running while hidden and then the menu bar item is the only evidence. Hiding otherwise dims the glyph, which makes the menu bar the confirmation that ⇧⌘J landed, for a keypress whose whole effect is that something disappears.
- All three are template images, which is also why practice is a DOT and not a colour: a coloured menu bar icon stops following the menu bar. Nothing animates there — a moving icon is a thing to look at, and this is a thing to notice. The icon is set only when the state changes, because the refresh runs three times a second and re-setting it is the menu bar item being rebuilt under the user.

- Milestone 16: yield to notifications. The window floats at level 25 and a banner is drawn at 21 — BELOW it, measured — so when one lands over the same corner the app is what is covering somebody's mail. It fades to 0.15 while something is over it and comes back. Fading, never moving: moving would fight per-app position memory for ownership of where the window lives, and would then owe two more answers with nothing to base them on (when to move back, and where).
- NO PERMISSION, and it was measured with an ungranted identity rather than from a terminal that inherits its editor's grant — which was the first reading and answered the wrong question. A throwaway ad-hoc-signed bundle (com.lyrisync.probe.wl16, never granted anything): CGPreflightScreenCaptureAccess 0 before AND after, 166 windows returned, owner/PID/layer/bounds all present, kCGWindowIsOnscreen present whenever true, no new on-screen window for 10s, no TCC log line naming the probe. Bundle deleted afterwards and it left no TCC entry.
- Exactly ONE field is withheld and it is the one that would deserve a prompt: kCGWindowName, the window's TITLE, absent for 160 of 166. The app has no use for it — it needs to know a notification is on screen, not what it says — and two tests enforce that, one forbidding the module to mention kCGWindowName and one forbidding every screen-capture entry point including CGWindowListCreateImage. Those tests scan the module's SYNTAX (attributes, names, imports, non-docstring strings), not its text, because the docstring has to name the very symbols they forbid in order to record the finding — the same trap test_packaging.py documents for the version literal.
- Keyed on the BUNDLE IDENTIFIER via the owner PID, never kCGWindowOwnerName, because that name is LOCALISED: "Notification Centre" here, "Notification Center" on a US system. A string match on it would have worked for whoever wrote it and silently never fired for half the people who ran it — the worst failure shape available, a layer that is simply off for reasons nobody can see.
- com.apple.controlcenter is deliberately NOT in the set, and it is the trap this walked into first: Control Centre owns ELEVEN permanently on-screen windows, one per menu bar item, so an app that yielded to it would fade on its first poll and never come back. There is a test.
- Banners and the Notification Centre PANEL are the same window — #54338, always present in the full list, flipping kCGWindowIsOnscreen from absent to true for the ~5.1s a banner is up and for as long as the panel is open (opened and closed by clicking the clock, measured). One rule, not two. kCGWindowIsOnscreen being "optional" in the docs means present only while true, which is what made the first probe look like a permission difference when no banner was up.
- The rectangle macOS reports is THE WHOLE DISPLAY — (0,0 1710x1107) on a 1710x1107 screen, for a top-right banner and for the full-height panel alike. There is no public way to find the banner's own rect that does not involve capturing its pixels, which is precisely what would need the permission this avoids. So the intersection is computed against what the system actually reports and that is stated rather than dressed up: today it discriminates by DISPLAY (a banner on the built-in screen does not fade a window on an external one), and it narrows for free if Apple ever reports something tighter. A banner rect guessed from where banners usually appear would be a number picked by eye, in a project where the scrim alpha and the tint chroma are not.
- Qt's geometry and CGWindowList's bounds are ONE coordinate space, checked rather than assumed because 12a found Qt disagreeing with the screen by the menu bar height: in one process, for one window, frameGeometry() (1240,40,460,200) against CGWindowListCopyWindowInfo (1240,40,460,200), dx=0 dy=0. So there is no conversion in the code.
- Rect intersection now lives in geometry.py and flight.py's private copy delegates to it. Two callers asking the same question is two places for the boundary case (touching edges) to be decided differently.
- The ceiling is ABSOLUTE (0.15), not a factor on the user's opacity: a factor would send a window already dimmed to the floor down to 0.04, which reads as having closed. min(theirs, ours), so yielding is never BRIGHTER than what was asked for — and with the opacity floor at 0.25 that min never picks their side today, which is exactly why it is pinned in a test rather than left to the floor staying put.
- THE MEASUREMENT THAT WAS OF THE WRONG QUANTITY. First pass reported the banner's "text contrast" collapsing to 1.50:1 under a fully opaque window — clean, plausible, repeatable, and not what it claimed. It sampled the whole window rectangle, where most pixels are the app BEHIND the banner, so "text" was the banner's pale body and "background" was a dark editor. Cropped to the banner, its own ink-against-body contrast RISES under our pale panel: 8.32:1 alone, 8.98:1 yielded, 12.90:1 fully covered, and swept 0→1 it never approaches 4.5:1. LOOKING AT THE CAPTURE is what found it. So no legibility threshold picks this ceiling, and claiming one did would be inventing a measurement that says something else.
- What the yield actually buys, measured from real pixels: interference from the bare banner (mean |channel diff| over the window's rect) 132.7/255 at full opacity → 20.2/255 yielded, 84.8% gone; and the banner's SEPARATION from the app behind it — what makes it read as a floating object — 9.91:1 alone, 1.50:1 at full opacity, 7.63:1 yielded. The value itself is set by eye at the point where the window has plainly receded without appearing to have gone, with the measurement taken afterwards to check it rather than to justify it: 12b's order.
- The poll is a repeating QTimer at 300ms, running only while the layer is on AND the window is showing. The monitor's own 300ms tick CANNOT carry it: position updates stop the moment nothing is playing, which is exactly a moment when the window is still on screen and still in the way — a layer that worked only during playback would be milestone 14's lesson again. Measured in the real app: 0.105ms CPU per poll, 0.035% of one core, against 2.3% for the line change. OnScreenOnly rather than the whole list is most of that (0.123ms against 2.227ms).
- Traced live, one real banner: appears 1.12s, fade begins 1.21s, fully yielded 1.45s (opacity 0.149); clears 6.44s, fade back begins 6.63s, fully restored 6.87s. 90ms and 190ms from the change to the fade starting — inside one poll both ways — and 240ms of travel against 260ms nominal.
- THREE things now scale the window's opacity and they compose in ONE multiply: the user's setting, the yield level, the flight. Each used to call setWindowOpacity itself, which worked only because no two were ever true at once — a fade to a notification landing during a flight would have been the first pair. Hiding gives the yield back BEFORE the flight borrows the opacity, because the flight restores its own factor and not this one, so a window that went away faded would come back faded.
- A sync pass refuses the fade (principle 6: the user is tapping a button on this window once per line), and the refusal is asked before every poll rather than only as a gate — a pass that starts while the window is already faded has to hand the opacity back, or it stays faint for a banner long gone. Every refusal names itself from the rule that decides it, app_positions.py's shape.
- windowOpacity() does not hand back what it was given: Qt stores it as 8-bit alpha, so 0.15 reads as 38/255 = 0.14902. Found by the first version of the Qt tests failing on the third decimal, and the tolerance for reading a real window's opacity ever since.

Parked
- Album-art background (the tint shipped in 12b; the artwork itself as a background did not)
- Multiple colours or gradients from one cover, per-song colour overrides
- Karaoke word-by-word
- Side panels
- Japanese romanisation
- Configurable hotkey bindings, and any hotkey beyond show/hide
- Focus fade
- Yielding to anything beyond the notification system (full-screen video, other apps' floating panels, screen sharing) — each needs its own answer to "whose window is that", and none is measured
- A different yield ceiling per appearance: light mode over a dark banner is already the worst pairing of panel and backdrop
- Publishing user syncs to LRCLIB
- Starting a sync mid-song, partial saves, per-line editing/nudging of an existing sync

- End every session by committing the work with a descriptive message and pushing.